<main>
  <div class="container">
   <div class="row">
   <%= form_with model: @genre, local:true do |f| %>
   <tbody>
    <tr>
     <td class="col-lg-8"><%= f.label :ジャンル名 %></td>
     <td class="col-lg-3"><%= f.text_field:name %></td>
     <td class="col-lg-2"><%= f.submit "変更を保存",class: "btn btn-sm btn-success" %></td>
    </tr>
   <% end %>
   </tbody>
   </div>
   </div>
  </div>
</main>
