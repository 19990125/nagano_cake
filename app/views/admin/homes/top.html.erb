<main>
 <p id="notice"></p>
  <div class="container">
  <div class="row">
  <div class="col-md-10 offset-md-2 mx-auto">
    <h4>注文履歴一覧</h4>

    <thead>
      <tr>
        <th class=col-md-1>購入日時</th>
        <th>購入者</th>
        <th>注文個数</th>
        <th>注文ステータス</th>
      </tr>
    </thead>

     <tbody>

      <td><%= @customer.id %></td>
      <td><%= @customer.last_name %> <%= @customer.first_name %></td>
      <td><%= @customer.last_name_kana %> <%= @customer.first_name_kana %></td>
      <td><%= @customer.postal_code %></td>
      <td><%= @customer.address %></td>
      <td><%= @customer.telephone_number %></td>
      <td><%= @customer.email %></td>
      <td><%= @customer.is_deleted_i18n %></td>
     </tbody>
     <td><%= link_to "編集する", edit_admin_customer_path(@customer),class:"btn btn-sm btn-success" %></td>
     <td><%= link_to "注文履歴一覧を見る", admin_homes_path,class:"btn btn-sm btn-success" %></td>
  </div>
  </div>
  </div>
</main>